发送的测试文件。

方案一：
建立好连接之后把文件所有内容读入一个string，一次调用TcpConnection::send()发送，不用担心文件发送不完整。
缺陷：内存消耗与（并发连接数*文件大小）成正比，如果文件过大只建立几个并发连接就会耗空所有内存。

方案二：（未实现）
为解决内存占用过多的问题，采用流水线思想，每次发送64k直到数据发送完。
需要额外的手段来记录每个TcpConnection的上下文。
缺陷：每个连接占用一个额外文件描述符，在高并发下64k内存也是不小开销。

